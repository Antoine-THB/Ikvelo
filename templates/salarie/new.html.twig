{% extends 'baseAdmin.html.twig' %}
{% block title %}Salarié{% endblock %}

{% block body %}
    <h1>Salarie creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('salarie_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
