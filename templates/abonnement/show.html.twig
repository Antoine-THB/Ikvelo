{% extends 'base.html.twig' %}
{% block title %}Abonnement{% endblock %}

{% block body %}
    <h1>Abonnement</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonnement.id }}</td>
            </tr>
            <tr>
                <th>Début</th>
                <td>{{ abonnement.DateDebut.format('d/m/Y') }}</td>
            </tr> 
            <tr>
                <th>Fin</th>
                <td>{{ abonnement.DateFin.format('d/m/Y') }}</td>
            </tr> 
            <tr>
                <th>Montant</th>
                <td>{{ abonnement.montant }}</td>
            </tr>
            <tr>
                <th>Indemnisation</th>
                <td>{{ abonnement.indemnisation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('front_index') }}">Retourner sur votre profil</a>
        </li>
    </ul>
{% endblock %}
