{% extends 'baseGestion.html.twig' %}

{% block body %}
    <h1>Salarie</h1>
    <div class="grid-2 .has-gutter">
        <div class="liste-accueil">
            <table>
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ abonnement.id }}</td>
                    </tr>
                    <tr>
                    <th>Salarie</th>
                        <td>{{ abonnement.idSalarie }}</td>
                    </tr>
                    <tr>
                        <th>Date début</th>
                        <td>{{ abonnement.DateDebut.format('Y/m/d') }}</td>
                    </tr>
                    <tr>
                        <th>Date fin</th>
                        <td>{{ abonnement.DateFin.format('Y/m/d') }}</td>
                    </tr>
                    <tr>
                        <th>Montant</th>
                        <td>{{ abonnement.montant }}</td>
                    </tr>
                    <tr>
                        <th>Indemnisation</th>
                        <td>{{ abonnement.indemnisation }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="liste-accueil" >
            <embed src="\uploads\brochures\{{ abonnement.justificatif }}" type="application/pdf" width="100%" height="400px">
        </div>    
    </div>
    <ul>
        <li>
            <a href="{{ path('gestion_validation_index') }}">Retour</a>
        </li>
    </ul>
{% endblock %}
